The queue requires an explicit constructor to be initialized appropriately.
The array should return false because the array is already full.
When you poll an empty FixedArrayQueue the result is null.
They all operate at O(1) time except asList which is O(n) time dependent.
